# CMakeList.txt : CMake project for two_dim_dynamic_array, include source and define
# project specific logic here.
#
cmake_minimum_required (VERSION 3.8)

project ("two_dim_dynamic_array")

# Add source to this project's executable.
add_executable (two_dim_dynamic_array "main.cpp" )
